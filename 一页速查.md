# 蒸馏 Skill 一页速查

> 打印或截图保存。复制 paste 即用。

---

## 🔑 两个 API Key

| 服务 | 申请入口 | 作用 |
|------|---------|------|
| DashScope（阿里云） | https://dashscope.console.aliyun.com/overview | ASR 语音→文字 |
| TikHub | https://api.tikhub.io | 获取抖音评论区数据 |

---

## 🔧 mcp.json 配置

```json
{
  "mcpServers": {
    "douyin-mcp": {
      "command": "uvx",
      "args": ["--with", "mcp<2.0", "douyin-mcp-server"],
      "env": { "DASHSCOPE_API_KEY": "你的 Key" }
    },
    "tikhub-douyin-sdk": {
      "command": "python",
      "args": ["路径/server.py"],
      "env": { "TIKHUB_API_KEY": "你的 Key" }
    }
  }
}
```

---

## 🧬 蒸馏六步

```
① 收链接 → 用户发抖音链接
② 解析视频 → douyin-mcp: parse_douyin_video_info
③ 转录文字 → douyin-mcp: extract_douyin_text (≤5min) 或 分片 ASR (>5min)
④ 拉评论 → TikHub: get_video_comments(aweme_id, count=50)
⑤ 分析 → 用户告诉 AI 这次看什么维度（不预设规则）
⑥ 输出 → 按用户指定的格式交付
```

---

## ⚡ Agent 启动话术

> "你是一个抖音评论区蒸馏助手。你的工具有 douyin-mcp（视频解析+转录）和 TikHub（评论区采集）。每次蒸馏前先问我这次看什么维度。不预设分析规则，不替我做过滤。"

复制到你的 Agent System Prompt。

---

## ⚠️ 三个坑

1. **视频 > 5 分钟**：不能用 `extract_douyin_text`，必须下载 → 提取音频 → 120s 分片 → `recognize_audio_file`
2. **mcp 版本锁**：`--with mcp<2.0` 必须加，否则 uvx 拉到 mcp 2.0 会崩溃
3. **TikHub 限流**：高频调用间隔 ≥ 30 秒
